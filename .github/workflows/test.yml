name: Test Lightning Runner
on: workflow_dispatch
jobs:
  test:
    runs-on: [self-hosted, lightning-fast]
    steps:
      - run: echo "Hello from Lightning Runner!"
      - run: sleep 30  # Keeps runner active longer for screenshot
      - run: echo "Done!"
